{
  "name": "pod-data",
  "description": "Pod Data is an unofficial checklist for tracking your completion progress in NieR: Automata. Mark various items, quests, and achievements as completed, to help keep track of what you've done and what still needs to be accomplished.",
  "private": true,
  "packageManager": "pnpm@10.26.2",
  "pnpm": {
    "onlyBuiltDependencies": [
      "@parcel/watcher",
      "esbuild"
    ]
  },
  "scripts": {
    "build": "nuxt build",
    "build:quiet": "node --disable-warning=DEP0155 ./node_modules/nuxt/bin/nuxt.mjs build",
    "dev": "nuxt dev",
    "dev:prod": "nuxt dev --dotenv .env.prod",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "supabase:types": "npx supabase gen types typescript --local > app/types/database.types.ts",
    "supabase:types:prod": "node -e \"require('dotenv').config({path:'.env'});require('dotenv').config({path:'.env.prod'});require('child_process').execSync('npx supabase gen types typescript --project-id '+process.env.SUPABASE_PROJECT_ID+' --schema public > app/types/database.types.ts',{stdio:'inherit',shell:true})\"",
    "supabase:dump": "supabase exec db dump -f db_schema.sql"
  },
  "devDependencies": {
    "@mdi/font": "^7.4.47",
    "@nuxt/fonts": "^0.12.1",
    "@nuxthub/core": "0.10.4",
    "@nuxtjs/seo": "^3.3.0",
    "@nuxtjs/supabase": "^2.0.3",
    "@nuxtjs/turnstile": "^1.1.1",
    "dotenv": "17.2.3",
    "nuxt": "4.1.3",
    "nuxt-og-image": "^5.1.13",
    "prettier": "^3.7.4",
    "sass": "^1.97.2",
    "vuetify-nuxt-module": "^0.19.2"
  },
  "dependencies": {
    "@vercel/analytics": "^1.6.1",
    "@vercel/speed-insights": "^1.3.1",
    "vuetify": "^3.11.6"
  }
}
